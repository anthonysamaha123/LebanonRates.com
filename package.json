{
  "name": "lebanon-rates",
  "version": "1.0.0",
  "description": "Lebanon Daily Rates Tracker - Currency, Fuel, and Gold Prices",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "fetch-data": "node scripts/fetch-data.js",
    "dev": "node build.js && npx serve dist",
    "watch": "node build.js --watch"
  },
  "keywords": ["lebanon", "exchange-rates", "currency", "lira"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "date-fns": "^2.30.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}