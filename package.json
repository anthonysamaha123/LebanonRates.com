{
  "name": "lebanon-rates",
  "version": "1.0.0",
  "description": "Lebanon Daily Rates Tracker - Currency, Fuel, and Gold Prices",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "fetch-data": "node scripts/fetch-data.js",
    "dev": "node build.js && npx serve dist",
    "watch": "node build.js --watch",
    "start": "node src/server.js",
    "test": "jest",
    "test-api": "node scripts/test-api-local.js",
    "monitor-api": "node scripts/monitor-api.js",
    "store-history": "node scripts/store-gold-history.js",
    "build-seo": "node build.js --seo-only"
  },
  "keywords": [
    "lebanon",
    "exchange-rates",
    "currency",
    "lira"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.18.1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "date-fns": "^2.30.0",
    "express": "^5.2.1",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "serve": "^14.2.1"
  }
}
